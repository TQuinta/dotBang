<div class="mid-column">
    <div class="author-top-middle-column">
      <%= render "author_avatar" %>

      <%= render "post_icons" %>

    </div>
    <div class="mid-column-content">
      <div>
        <h1><%= @post.title %></h1>
        <h2><%= @post.blurb %></h2>
        <p><%= @post.postable_type %></p>
      </div>
      <div>
        <p><%= @post.content %></p>
      </div>
    </div>
  </div>
